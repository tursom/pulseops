services:
  pulseops:
    build:
      context: .
      dockerfile: Dockerfile
    image: pulseops:latest
    ports:
      - "8088:8080"
    volumes:
      - ./configs:/app/configs:ro
      - ./logs:/app/logs
    restart: always
